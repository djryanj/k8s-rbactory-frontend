apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: k8s-rbactory-frontend
  annotations:
    config.kubernetes.io/local-config: "true"

namespace: k8s-rbactory

commonLabels:
  app.kubernetes.io/name: k8s-rbactory-frontend
  app.kubernetes.io/component: api-server
  app.kubernetes.io/part-of: k8s-rbactory

commonAnnotations:
  app.kubernetes.io/managed-by: kustomize

resources:
  - deployment.yaml
  - namespace.yaml
  - service.yaml

images:
  - name: k8s-rbactory-frontend
    newName: ghcr.io/djryanj/k8s-rbactory-frontend
    newTag: latest

replicas:
  - name: k8s-rbactory-frontend
    count: 1
